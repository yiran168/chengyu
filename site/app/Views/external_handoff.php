<section class="connect-shell panel glass" aria-labelledby="handoff-title"><span class="eyebrow">A SECURE CONTINUATION</span><h1 id="handoff-title"><?= e($title) ?></h1><div class="identity-preview"><?= icon('shield') ?><div><strong><?= e($host) ?></strong><small><?= t('Check the destination before continuing.') ?></small></div></div><p><?= t('Your request is ready. Continue to the identity or payment provider. You will return here afterwards.') ?></p><a class="btn full" href="<?= e($target) ?>" rel="noreferrer noopener" referrerpolicy="no-referrer" data-external-handoff><?= t('Continue securely') ?><?= icon('arrow') ?></a><p class="hint"><?= t('If automatic navigation is unavailable, use the continue link. This page does not confirm a payment.') ?></p><a href="<?= e(url()) ?>"><?= t('Back to home') ?></a></section>
