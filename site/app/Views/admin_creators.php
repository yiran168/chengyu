<?php $author=\Chengyu\Core\Input::integer($_GET['author_id']??0);$income=$a->creator->page($author,\Chengyu\Core\Input::integer($_GET['page']??1,1,100000)); ?>
<div class="admin-title"><div><span class="eyebrow">CREATOR ECONOMY / REAL ORDERS</span><h1><?= t('Creator revenue') ?></h1><p><?= t('Manage approvals in member profiles. Revenue is separate from referral commission.') ?></p></div><a class="btn secondary" href="<?= e(au('settings',['group'=>'commerce'])) ?>"><?= icon('settings') ?><?= t('Revenue rules') ?></a></div>
<div class="creator-policy panel glass"><div><span class="eyebrow">SHARE</span><strong><?= number_format((int)$s->get('creator_share_bps')/100,2) ?>%</strong><small><?= t('Of revenue after referral costs') ?></small></div><div><span class="eyebrow">HOLD</span><strong><?= (int)$s->get('creator_hold_days') ?> <small><?= t('days') ?></small></strong><small><?= t('Not a promise of automatic external payout') ?></small></div><div><span class="eyebrow">MODULE</span><strong><?= t($s->enabled('creator_income')?'Enabled':'Disabled') ?></strong><small><?= t('Refund reversals remain active when disabled') ?></small></div></div>
<section class="panel section"><form method="get" action="<?= e($a->basePath.'/admin/index.php') ?>" class="admin-search"><?= hidden('tab','creators') ?><input class="input" name="author_id" type="number" min="0" value="<?= $author ?>" aria-label="<?= t('Author ID (0 for all)') ?>"><?php button('Search','secondary','search'); ?></form><?php partial('income_rows',['income'=>$income,'adminIncome'=>true]);partial('pagination',['pagination'=>$income,'tab'=>'creators','params'=>['author_id'=>$author]]); ?></section>
<p class="alert info"><?= t('Settlement is requested by the author. Payout requests are reviewed in Withdrawals after actual external payment. Never mark a transfer paid before sending it.') ?></p>
