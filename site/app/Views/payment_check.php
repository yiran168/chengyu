<?php if($s->get('epay_query_enabled') && $order['kind']==='topup' && $order['gateway']==='epay' && in_array($order['status'],['pending','expired'],true)): ?><?= form_start('payment_reconcile').hidden('order_id',$order['id']) ?><?php button('Check payment status','secondary small','refresh'); ?><p class="hint"><?= t('Checks the original merchant order. A browser return alone never marks an order paid.') ?></p></form><?php endif ?>