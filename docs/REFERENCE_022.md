# 三套主题源码再读：0.22

阅读与检索日期：2026-09-13。本轮再次实际读取子比、Shane 和 JustNews 的 PHP 模板/函数，以及对应 CSS；不是只分析 JustNews。具体文件、范围与整文件 SHA-256 见 [读取记录](evidence/022/reference-source-review.json)。只读参考，没有执行其授权代码，也没有把这些代码、图片、字体或品牌标记复制到发布包。

| 来源 | 本轮实际代码与观察 | 本项目的独立实现 |
| --- | --- | --- |
| 子比本地 8.9 | `zib-header.php` 的页头与手机菜单；`zib-posts-list.php` 的单图、纯文字、多图判断；`zib-theme.php` 的正文图片和至多四图缩略图；`main.css` 的菜单位移、缩略图间距和轻微悬停 | 导航共享权限树；正文媒体 ID 解析；列表批量加载公开图片；短距离菜单淡入 |
| RiPro V5 子主题 Shane | `header/menu.php` 的导航与独立账户操作、菜单缓存；`loop/item.php` 的网格/叠图/列表/标题模式；`entry-media-part.php` 的格式图标、懒加载与媒体预览；`shane_home.css` 的 VIP 按钮及局部曲线 | 保持栏目、标题、摘要、统计的固定层次；文章图片排版与商品分开配置；动效沿用统一曲线，不增加常驻媒体自动播放 |
| JustNews 本地 6.16.7 | `header.php` 的三层菜单；`nav-walker.php` 的分栏、图片/图标与活动项；`loop-multimage.php` 的多图列表、摘要及作者；`style.css` 的菜单间距与标题字重 | 后台父级/图片/说明/分栏配置；桌面、手机与无 JS 共用模板；公开正文和手动图片共同驱动列表 |

Shane 目录仅含子主题。`zb_get_archive_item_config`、`zb_get_thumbnail_url` 等调用能说明界面接口，但缺少 RiPro 父主题实现，不能据此声称审计了全部 RiPro 核心。本次 CSS 按选择器和短文件阅读，不等于逐行审计整个主题。

## 官方网页再次核对

- [子比官方日志](https://www.zibll.com/375.html/comment-page-4)：V9.1（2026-09-01）提供菜单显示条件。本版沿用已有登录/VIP/认证限制，并把它应用到父子菜单。该日志也列出第三方登录、海外支付等扩展，本版没有把它们标记为全部完成。
- [RiPro 官方产品页](https://ritheme.com/theme/ripro-v5.html)：本次搜索取得 V10.3（2026-08-18）及 V10.1 的 UI 更新记录，包括统一缓动、较柔和灰阶和局部阴影；直接打开返回 403，因此不声称本次成功浏览了其全部演示页。只提取通用设计原则。
- [JustNews 官方文档](https://www.wpcom.cn/docs/justnews)：实际打开并阅读高级菜单的二级分栏、三级内容、图片样式，以及首页图文列表说明。本版保留清晰的信息层次和人工配置方式。

界面的“人味”主要来自真实内容、克制的留白、稳定的标题与元信息位置，以及与按钮含义相关的图标。本版继续减少装饰对阅读的占用；新增相机图标独立生成，既能当编辑入口，也可在后台被站长替换。

## 主机约束核对

[蓝队云免费主机 FAQ](https://notify.landui.com/help/show-12693)明确描述 Windows 环境、FTP/`wwwroot`、无 SSH、不能单独升级环境、数据库地址为本机，以及底部链接和续期要求。本项目继续提供平铺上传包、IIS 配置与浏览器安装路径。其通用付费主机页面不用于推断免费套餐容量。

萌哒云的可核验信息仍来自用户截图：SSD 2 GB、MySQL 1 GB、月流量 780 GB，以及面板 PHP 7.4/8.0/8.1/8.2 选项。这不能证明账号已启用所需扩展、TLS 或目录保护。没有登录真实账号做部署，VPS 也未做实际安装，因此发布报告保留这些未验证项。
