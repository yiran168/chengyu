# 0.20.0 实际测试记录

2026-09-09，数据库结构 v12。所有本地测试使用隔离数据，不接触用户的生产站点。

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| Windows 原生 PDO SQLite | PHP 7.4.33 / 8.0.30 / 8.1.34 / 8.2.33 / 8.3.33 / 8.4.25 / 8.5.10 各 587/587，stderr 为空 | evidence/020/native-matrix.json、php-*.json |
| 真实 PHP HTTP 请求 | 873/873 | evidence/020/http-final.json |
| Chromium 实页、表单和动效 | 87/87，截图 12 张 | evidence/020/browser-final.json、previews/020-* |
| PHP 语法 | 7.4 与 8.5 各 283 文件通过 | evidence/020/syntax.json |
| JavaScript 与弹簧数学 | 18 文件语法、42/42 不变量 | evidence/020/syntax.json、spring-final.json |
| 0.18 与 0.19 升级 | PHP 7.4、8.5 四组升级通过 | evidence/020/upgrade-from-018.json、upgrade-from-019.json |

浏览器验证真实 Cookie 登录、图标筛选、内置预览、上传 ID 回填、上传预览、保存和恢复默认、四种宽度、归档原生筛选、后台曲线保存后前台生效、系统减少动态效果、按钮波纹尺寸、投票、问答、编辑器和无 JavaScript 阅读。Windows Chrome 150 无头模式使用软件渲染，避免原生遮挡检测停帧；截图没有用概念图替代实页。

发现并修复按钮波纹占据布局导致点击丢失、上传字段无法回填、跨页面快照导致帧冻结。HTTP 中的 TOTP 重放测试现在重用真正的激活码，避免恰好跨 30 秒窗口后把新码误判为重放。

升级演练保留余额、订单、付费权限与受保护正文；0.19 加密资源链接仍能读取；旧站上传封面不被替换。备份白名单包含新表。

本版 Linux 原生 MySQL/SQLite 的 GitHub CI 正在准备，当前可核实的历史通过记录属于 0.19 提交 006d841，不作为 0.20 通过证据。发布前会补充实际运行结果。

两家免费虚拟主机、用户 VPS、真实支付/邮件/对象存储账号未进行线上部署联调。主机限制按官方资料及用户截图整理，并提供安装/后台运行检测。PHP 七版本使用同一业务路径，没有按版本关闭模块；能否部署仍取决于该实例的扩展、权限和配额。不能将功能矩阵中未实现的模块或外部接口称为已经完成。
