# 0.23.1 · 2026-09-19

- 修复数据库、SMTP 与后台密钥保存时首尾空白被裁剪；保留编码、长度及控制字符校验，空字符串继续保留原密钥。
- 修复不存在的文章编辑链接误打开新建表单，改为 404；已有文章的权限检查不变。
- 新增 4 项核心回归和 21 项 HTTP 检查；7 个 PHP 版本每版 624/624，HTTP 1024/1024，14 组原生 MySQL/SQLite CI 各 624/624。
- 结构仍为 v15，保留已有配置、数据库和上传文件升级，无需重新安装。

# 0.22.0 · 2026-09-13

- 再次实际阅读子比、Shane、JustNews 导航、列表、正文图片函数和样式，记录源码范围及哈希；独立实现通用设计。
- 新增三层图文导航与后台编辑，父项可见性继承；限制循环和过深子树；权限重检、幂等与修订冲突处理。
- 正文可插入本站公开图片，支持说明与大图查看；文章列表支持手动或公开正文2–4图，图片隐私变更实时过滤。
- 新增独立二次元相机图标；81逻辑符号中8个生成图标可在后台逐项替换。
- 修复行内代码中的链接被激活、下拉菜单与折叠高度动画竞争、递归模板未传当前栏目及移动父菜单图片遗漏。
- 结构v14；旧菜单、正文、订单、余额、权益与资源保留。验证见TEST_REPORT.md。

# 0.21.0 · 2026-09-13

- 重新阅读子比、Shane、JustNews 及附带插件的30个源码文件，附逐文件行段和SHA-256记录，独立实现通用交互与排版。
- 新增快讯前后台、日期时间线、月份/搜索分页、独立详情、侧栏、RSS、站点地图；发布时刻按请求判断，幂等与修订冲突保护。
- 新增文章来源、版权模板、作者卡、同分类上下篇及相关阅读；后台可配置摘要、封面和列表左右图/纯文字。
- 新增独立二次元快讯图标；共80个逻辑符号、7个生成图标可自定义。保持文字层级与克制悬停。
- 修复内容保存对过期角色的信任、日期年份边界、文章开关误伤商品封面；增量结构v13。
- 验证详情见TEST_REPORT.md，未把免费主机实机或全部外部接口标记通过。

# 0.20.0 · 2026-09-12

新增视觉素材库、6个生成图标、6张独立封面、内容列表首页、服务端分类/排序/翻页、无图条目、年月归档、评论分页/排序和实际PHP运行限制报告。修复上传回填、按钮波纹撑大点击区域、分类区块重复查询与数量限制。最终结构v12，完整浏览器91项通过，14组原生数据库CI通过；准确历史证据见evidence/020。早期开发日志中的v11与“浏览器未完成”已由最终验收取代。

# 0.19.0 / 2026-09-08

新增多版本资源与反馈、组合筛选、22种编排区块、响应式轮播/按钮组、原创二次元站标与插画。修复Windows安装路径、文本分片MIME、SKU标价排序、布局布尔校验，公开包使用每站独立安装口令。实际验证见TEST_REPORT.md，部署方式见DEPLOYMENT.md。

# 0.17.0 / 2026-09-07

## 0.18.0 — 2026-09-07

基于实际 0.17.0，数据库增量 v10。先复跑 511 项原核心基线。

- 修复统一 HTTP 客户端的大小写重复请求头、表单体积限制与 HEAD 长度歧义；修复关键词 `0`。
- 新增 Meilisearch 公共元数据连接、合并同步队列、异步确认/修订竞争保护、受限后台推进和本地权限回查。
- 实物订单最多 20 个包裹，分别维护运单与轨迹；保留旧运单，新增版本/归属/幂等检查。
- 新增加密恢复保险库、数据库一致性快照、可选本地媒体和空目标 CLI 恢复；恢复后暂停任务/对外服务。不是自动异地灾备。
- 邮箱验证码同 IP/收件人的 10 分钟/小时/日限流与冷却原子处理。
- 新工作台响应式视觉、指针光效、减少动态效果、双语；修复备份与双因素区域样式冲突。
- 当前执行结果：核心 558/558，本地 HTTP 804/804，离线浏览器 498/498，弹簧 42/42。PHP 8.4.23/测试 FFI SQLite；原生 PDO、MySQL、多版本、线上外部服务未验收。

以实际0.16.0完整包为基线，数据库从v8增量迁移至v9，不覆盖同名旧版本。

- 修复访客影子账号进入会员人数统计、译文元数据查询过量取正文、媒体失败清理时Windows句柄未关闭、编排页canonical丢失位置等问题。
- 新增便携倒排索引、分批重建、权重排序及修订/译文联动，公开元数据与受保护正文分离。
- 布局增加独立草稿、发布、放弃、20次历史恢复和并发版本冲突保护。
- 新增课程/章节/预览/私有资源/私人笔记与进度，复用内容权限、退款撤权和媒体交付。
- 新增物流轨迹、手工事件和快递100查询适配；加密单号/轨迹、限流及串单检查，不自动确认收货。
- 新增标准CSV账单差异报告，校验账期、金额、交易号和状态，幂等导入且不操作资金。
- 新增前后台客服工单、模板、通知、状态与优先级，所有者权限与重复提交/修订保护。
- 独立前后台语言、支付宝PAGE模式、集中导航白名单、共享新工作台/课程视觉，动态文案补译。
- 重新读取ICU三分卷并取得官方早期及最新日志，形成有证据边界的历史功能族对照。完整缺口和真实测试方法见当前功能矩阵与报告。

以下是保留的历史记录；历史版本“未完成”表述只代表当时状态，以当前FEATURE_MATRIX.md为准。

# 0.15.0 / 2026-09-06

- 修复已撤销会话改密、超长 bcrypt 截断密码二次确认、停用账户缓存资料更新。
- 新增带有网络边界保护的 HTTPS 传输层，三家授权码登录共用状态/PKCE/绑定/本地账户保护。
- 易支付查单与通知共用入账，查询审计、快照密钥、精确金额与原域约束。并发时 changed 返回本次是否真正入账，不仅根据最终 paid 状态猜测。
- 永久会员、重复权益购买保护、单件币种确认与整车三资产上限回滚。
- 加密配置快照导入/导出/差异/恢复、社区徽章全链路与对应前后台。
- 分组后台导航、搜索恢复、Alt+/ 快捷键、新卡片与有限次图标动效，共用曲线与降动态策略。
- 结构 v7；原站保留配置、密钥、数据库和存储升级，不重新安装。

# 版本记录

## 0.14.0 · 2026-09-06

**先修错。** 原 300 项核心通过后增加三条失败复现：低等级首次开通借用高级到期时间、停用账户兑换码、登录服务接受超过 bcrypt 上限的密码；修复后 303 项通过。浏览器回归发现 0ms 被折叠逻辑回退到默认值，改用统一时长，并允许后台保存零时长、清除其入场交错等待。

**安全中心。** RFC 6238 SHA1 时间型认证、密钥加密、恢复码摘要、重放保护、绑定/轮换/解除、会话撤销与审计；密码找回和暂停新绑定不移除已有保护。恢复工具只供 Web 根目录外本地命令使用，不提供 HTTP 万能解除。

**共用计价。** VIP 三档付款比例、限时优惠、较低价格择优、高级继承低级优惠；券范围/目标/开始/会员门槛、叠加开关、事务核销。列表、SKU、购物车、试算与成交共用 Pricing；原 Commerce 不再维护重复券计算。订单保存实际价格快照，单件确认上限保护和退款原额回退。

**前后台。** 新账户安全中心、定价工作台、订单优惠说明和报价反馈。连续布局重排保留组件键、展开状态和焦点，支持 Alt+方向键与播报；专注阅读不改权限和购买路径。低资源/减少动态效果时只收敛装饰。

**部署能力。** 新后台自检实际检查驱动、扩展、加密、参数绑定、回滚与存储，JSON 不含密钥和路径。CI 增加原生驱动硬校验，避免 FFI 回退被算作原生通过。生产不引入 Composer/Node/FFI 依赖。

**迁移与边界。** v6；179 项集中设置，36 项动效参数。保留旧数据/密钥，旧订单不重算。339 核心、458 本地 HTTP、263 离线浏览器、42 弹簧检查通过；原生数据库、多 PHP、真实主机和商户仍未完整验收。

## 0.13.0 · 2026-09-06

**先修复再扩展。** 在 0.12 基线通过后增加失败复现：密码确认使用陈旧缓存、私有附件关联只检查前 100 条、底层转账接受停用发送方；修复并保留回归。管理员置顶不存在评论返回明确错误，提现确认重新检查退款后的来源资产负债，迁移创建索引改为查询原生索引清单后判定。

**作者收益。** 新增独立 `earnings` 资产、订单比例/等待期快照、推荐佣金扣除后的净额应计、批量合资格结算、重复/并发保护、退款撤销/冲回和负收益。可选收益转余额；人工提现记录保存原资产来源，驳回正确退回。旧订单不补分成。

**付费投稿。** 管理员批准作者/独立比例；获准作者可设余额付费文章/帖子，仍需审核；旧管理员价格默认受保护。新增真实创作中心与后台作者收益列表，共用记录模板，不复制账本规则。

**内容发现。** 本人阅读记录、进度、容量裁剪、关闭并清空；关注作者内容流；同权限检索建议、200ms 防抖、IME、过期响应失效、键盘导航和文本安全渲染。

**视觉交互。** 统一引擎驱动导航指示线、表单反馈、图片显现；通知限制数量、可关闭、暂停计时和焦点恢复；异步表单延后显示忙碌装饰但不延后请求/响应。修复 search 输入框拦截 Escape、未关闭弹框的行为。动效实验室新预览区和 3 个开关。初始化隐藏页状态，尊重减少动态效果与全局时长 0。

**迁移与交付。** 结构 v5；174 项集中设置、35 项动效参数。原生数据库/全部 PHP/真实主机仍未验收，详见测试报告，不将离线浏览器结果冒充公网通过。

## 0.12.0 — 2026-09-06

基于可读取的 0.11.0 源码继续维护。先增加失败复现测试、修复，再扩展业务与前后台。

## 修复

普通作者编辑内容时保留管理员设置的价格资产/金额、有效期、VIP 门槛、口令、库存/交付参数、精选/置顶、发布时间和私有资源等受保护字段。管理更新不存在记录返回 404，不再假装成功。

登录在事务内校验当前密码哈希；并发改密后不再用陈旧哈希覆盖新密码。分类树限制父子类型、循环和深度，有内容或子级的分类不能改型破坏继承。折叠动画完成取消残留填充与内联高度，快速反向也按最后一次意图落定。

圈子成员管理增加超过 50 条后的分页，商品评价和成员列表复用统一分页组件并保留筛选上下文。

移除链接和提交后跳转的人为等待；修正移动后台/桌面菜单触发、焦点和无 JavaScript 可访问性。新增中文标签与表单提示。

## 业务

分类继承读/写权限、圈子发现/加入/到期/成员管理/退款；积分悬赏托管/采纳发奖/过期领取/管理员退回；单选升级多选与旧票回填；购买后自动生成邀请码及注册次数/退款逆向；经购买验证的评分与审核、退款后剔除公开评价。

新增 Categories、Bounties、Reviews 共用服务，扩展 Commerce、Threads、Invitations，而不是新建重复钱包/支付逻辑。数据库结构 v4，增加四表及相应字段。新增管理入口和 164 项/12 组设置。

## 动效

解析式物理弹簧与贝塞尔双路径；32 项统一动效参数，刚度/阻尼/质量、速度连续、欠/临界/过阻尼图与共同预览。指针动画收敛即停止，减少动态/隐藏页面清理。补充已授权图片灯箱、原生分享回退、反向折叠和移动导航键盘闭环。

## 验证

264 核心、326 本地 HTTP、171 离线浏览器、42 弹簧算法、121 PHP 与 8 JS 语法通过；34 张程序页面截图。测试数据库为真实 SQLite 测试适配，非原生 PDO/MySQL。PHP 8.4.23 实际运行，7.4–8.5 同路径但其他版本/14 组原生 CI 未执行；真实浏览器 URL 导航被策略阻止。

不是全历史模块完整复刻，剩余缺口与外部环境验收见当前功能表和测试报告。

---

# 0.11.0 — 2026-09-06

基线为本项目 0.10.0 完整包。先修已发现问题，再扩展模块；不是导入用户的原主题。

## 修复

退出登录清理所有临时授权与一次性展示内容；嵌套事务失败不允许被外层捕获后继续提交。用户改密/改邮箱与管理员改密码、邮箱、角色或状态时，吊销未用找回链接；仅改昵称不误吊销。管理员改邮箱清除验证时间。

受限内容的评论不再绕过正文权限输出；保留回复后可见的必要回复入口。审核拒绝已采纳回答时同步清除采纳标志。SMTP 已收到 DATA 成功后，不因 QUIT 阶段断开误判为发送失败。

异步提交与历史返回只恢复原本由提交过程临时禁用的按钮，不解禁原本禁用的控件；主题偏好在实际应用新主题后写入，避免异步转场保存旧值。截图不再因焦点或平滑滚动拍到中途页面位置。

## 新增

邮箱验证码注册与改绑、单次证明、用途/会话绑定、发送与验证限额；问答采纳与审核联动；一账号一票的单选投票、截止/结果隐私与首票后的选项锁定；可见正文阅读目录。

动效工作台、三套预设、五条预置曲线及自定义三次贝塞尔、曲线图/数值预览；统一的入场交错、卡片光点/透视、按钮磁吸、扫光、首屏视差、主题转场和弹层反馈。系统减少动态效果优先；低资源、触屏、离屏/隐藏状态做相应收敛。

## 工程与说明

新增 Verification、Threads、Motion，数据库结构 v3。曲线定义从 PHP 单一来源输出，设置部分保存复用整组验证；不复制主题业务。增加并发投票、验证码、本地 TLS SMTP HTTP 流程与浏览器动效测试。附新操作说明、当前测试证据和失败/未验收边界。

未实现完整付费圈、作者销售分成、第三方登录/短信、全套国际支付/自动原路退款、全部商城售后、全站自由设计和任意主机的自动证书部署。PHP 7.4–8.5 是相同功能目标，不是全版本实测声明。

---

# 版本变更

## 0.10.0 — 2026-09-06

基于实际交付的澄屿 0.9.0 源码继续开发，不是另一个未找到的同名 ZIP。

### 优先修复

移除 PHP 8.2 以下创建充值单的版本门槛，保留 HTTPS 与协议安全校验。修复对非付费内容构造零元订单绕过会员/回复/密码权限的路径。后台普通资料编辑不再隐式延长高级会员。

输入的数组/标量、密码零字节与长度、无 mbstring 时 UTF-8 截断等统一验证。登录切换清理旧购物车和密码授权、刷新 CSRF。新结构的内容、修订、布局和履约长字段避免只用 MySQL 64KB TEXT 上限。

库存操作集中到服务层，卡密存量实时同步，购物车整体事务和指纹重试保护；退款按原规格恢复未发货库存，已发卡不复卖。推荐佣金冲销不会因已提现而阻塞买家退款，保留人工债务对账边界。

### 新业务

实物/卡密多规格；三级独立期限会员；付费内容有效期；密码内容与文本修订；下载流水、日配额与单段断点续传；任务中心；邀请注册；专题、友情链接、公告；确认收货和物流信息更正。

### 新界面

加入布局工作台、三套起始组合、安全 JSON 导入/导出、组件调序/复制/设备显隐/入场效果。新增首屏艺术样式和密度选项，优化主题、玻璃、动效、后台导航搜索、移动表单。131 项设置/12 组，15 张实际页面预览。

### 升级与验证

增量迁移结构 v2，可重试但不提供自动降级。核心 158/158、本地 HTTP 199/199、浏览器 58/58、95 个 PHP 文件语法通过。七 PHP 版本 × 两原生数据库 CI 文件已提供但未远程执行；本地数据库为真实 SQLite 测试适配，非原生 PDO/MySQL。详见测试报告。

## 0.9.0 — 基线记录

提供独立内容社区、资产和轻量商城、5 主题及基本部署文档。原证据为核心 82、本地 HTTP 115、浏览器 26 项；这些只是历史基线，不是当前版本测试数。已知的版本支付门槛和单层会员/单规格限制在 0.10.0 中调整，未实现项以新版功能表为准。


### 0.15.0 发布前补充：保留严格 CSP 的外部继续页面

静态核查发现，原生表单直接 303 到外部服务可能受 `form-action 'self'` 限制；这是依据 CSP 文档识别的兼容风险，不冒称在本环境真实浏览器中复现。新增共享 HTTPS 目标校验与同源继续页，覆盖社交登录和收银台；无脚本时提供可操作链接，脚本可用时立即导航。拒绝非 HTTPS、URL 凭据、控制字符、片段和超长目标，不开放任意 URL 转发路由。新增 9 项核心检查和 14 项本地 HTTP 检查，全部保留既有断言。

## 0.16.0 / 2026-09-06

在 0.15.0 工程上增加访客收银台/总单、四种官方支付协议、按单核付/退款、PayPal Payouts、永久与剩余价值补差、运费/组合优惠、退货退款/同 SKU 换货、本地分片/私有 S3、中英/译文审核、多词相关度搜索、22 个页面插槽及交易/媒体后台。

修复较新数据库被旧代码接受、访客购买身份与登录边界、会员退款覆盖后授权益、签到回归、CLI 任务配置路径、任务端点状态码、新侧栏样式被基础 panel 规则覆盖导致移动端偏移。增加权限、幂等、协议、未知结果和媒体验证。

新外部服务默认关闭，真实商户/主机/云存储尚未验收；不是所有子比功能等价交付。

0.20 补充：后台视觉素材库（79 图标、6 个独立生成二次元图标、7 个页面位置）、6 张独立封面、导航图标、简洁阅读样式；修复上传控件回填与预览；升级至结构 v12。使用方法见 PLATFORM_020.md。

按钮波纹现在使用绝对定位，不参与按钮尺寸计算，修复按下后点击区域改变而导致提交失败。支持系统减少动态效果；页面跳转采用 CSS 入场，主题切换保留同页 View Transition。

## 0.23.0 · 2026-09-14

统一公开图片选择器覆盖图标、Logo、菜单、封面、正文、画廊和头像，支持搜索/游标分页/权限重检，不重复存储附件。数据库v15新增媒体索引；修复矮桌面固定栏按钮难以到达、通用弹窗压窄图片库、分页中文缺失。
