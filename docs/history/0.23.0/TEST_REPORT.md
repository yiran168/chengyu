# 0.23.0 实际测试记录

2026-09-14，数据库结构v15。测试使用独立数据库、随机预览账号和项目内临时目录，不读取生产配置。

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| Windows原生PDO SQLite | PHP7.4.33、8.0.30、8.1.34、8.2.33、8.3.33、8.4.25、8.5.10各620/620，stderr为空 | evidence/023/native-matrix.json、php-*.json |
| Linux原生MySQL8 / SQLite | 14/14组各620/620 | evidence/023/ci-verified.json、native-*.json |
| 真实PHP HTTP请求 | 1003/1003 | evidence/023/http-final.json |
| Chromium真实页面与表单 | 200/200，JavaScript错误0 | evidence/023/browser-final.json |
| PHP/JS语法 | PHP7.4与8.5各310文件，JS20文件 | evidence/023/syntax.json |
| 弹簧曲线不变量 | 42/42 | evidence/023/spring-final.json |
| 原始0.22升级 | PHP7.4、8.5各13项保留检查 | evidence/023/upgrade-from-022.json |

CI验证提交 `3a557a0f9f4f1e5eba5220139a09d87eb06ede7d`：[实际运行](https://github.com/yiran168/chengyu/actions/runs/34803427897)。逐一下载14份制品核对620项结果，未使用旧版结果替代。最终版PHP和JavaScript业务代码与该提交一致；随后调整图片库CSS宽度并在完整Chromium套件中重跑通过。后续文档/发布提交不修改业务逻辑。

新增8项核心测试检查当前账号状态、角色降级、自己的公开媒体、字段白名单、字面文件名搜索、跨用户限制、25张图片分页时插入新记录、选择前撤回、重复选择不复制媒体、关闭新上传后的复用和迁移可重入。保留旧模块全部服务检查。

HTTP通过真实上传构造13项媒体，检查CSRF、访客、普通成员与后台的访问范围、游标翻页、媒体转私密后的拒绝以及界面字段。首次测试把CSRF失效预期写成403，实际安全层返回419；纠正测试预期后完整1003项通过，未放宽安全规则。

浏览器验证图标替换与保存、清除、两个画廊字段独立回填、菜单图保存、文件名搜索、空状态、12项翻页与返回、断网后恢复搜索、取消不改原值、焦点返回、减少动态效果和无JavaScript回退。检查320/390/768/1440宽度，并复现及修复1254×564窗口下固定素材栏按钮难以点击的问题。另修正通用弹窗560px上限覆盖图片库940px布局，加入实际宽度断言。完整套件同时回归原有登录、前后台导航、动效、视觉素材、快讯、评论和文章图片等流程。

随包保留6张023前缀的新图片库预览，其中4张来自完整回归、2张来自agent-browser独立预览。022及更早截图保留原历史标记。没有以静态HTML填充或概念图替代实际浏览器验证。

升级演练由原始0.22上传包生成结构14旧库，再用当前代码迁移至15；余额、订单、购买权益、保护正文、加密资源、导航及图片默认字段保留。新增索引不改原文件。

两家免费主机具体实例、用户VPS安装、真实支付、外部SMTP和对象存储账号未实机联调。本地及CI通过不能替代具体实例的扩展、配额、TLS、回调和私有目录保护验证。PHP7.4–8.5使用同一套功能代码，功能边界见FEATURE_MATRIX.md。

包校验结果、准确字节数和SHA-256由打包工具另行生成，见Release的package-verification.json及SHA256SUMS.txt。
