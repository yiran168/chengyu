# 从 0.17.0 升级到 0.18.0

实际基线是用户提供的0.17.0完整包，数据库由 **v9到v10**。配置文件中的 `schema_version: 1` 是配置格式，不能改成10。此程序不导入WordPress或ICU数据库。

## 先备份，再覆盖

1. 暂停收款、退款/代付计划任务及用户写入，备份同一时点的数据库、旧代码、`app/config.php`、其中原始 `secret`、整个 `storage/` 及服务器规则。不要用新版本密钥替换旧密钥。SQLite要停止写入或使用一致性备份，不能遗漏WAL。
2. 在隔离环境先验证恢复。旧0.17没有本版整站快照工具，请使用主机面板/数据库工具备份，不要为了备份先在唯一生产库上升级。
3. 覆盖上传包的应用文件与资源，保留原配置和存储；旧站不用再次上传 `install/`。清理OPcache、浏览器和CDN缓存。
4. 首次请求执行顺序迁移至v10。迁移需要存储可写和数据库建表、加列、索引、改名等权限。MySQL DDL不保证整体事务回滚；失败时停写排查，不能手动提升版本号跳过迁移。
5. 检查后台系统自检与 `ACCEPTANCE_018.md`，再恢复交易和受认证的调度。外部搜索默认不启用，不会自动把内容发送出去。

## 数据变更

新增 `cy_search_outbox`、`cy_external_search_state`、`cy_backup_archives`。物流表新增 `package_key`、`package_label`，从每订单一条改为订单与包裹键联合唯一。旧运单ID、加密单号、轨迹、修订和关联人工事件保留，旧包裹键为 `primary`。SQLite在事务内重建该表并核对行数；MySQL逐步更新列和唯一索引。

没有新增账目入账动作，没有自动退款，没有替用户确认收货。多包裹仍附属于原有订单单位；它不是一张订单跨多个商品的装箱分配系统。原退款、会员补差和作者收益限制继续保留。

邮件发送次数默认IP为3/10分钟、30/小时、150/天，收件人为3/10分钟、原 `mail_limit` /小时、9/天；另外有60秒收件人冷却。旧 `mail_limit` 值会保留。各时窗是固定窗口，冷却是实际经过秒数；不是任意滑动窗口统计。已有邮箱验证、密码重置与改邮箱共用额度。

## 备份与恢复区别

升级是保留现有数据库继续迁移；离线恢复是把本版加密归档恢复到一个空数据库和全新存储目录。不要将恢复工具指向生产库。恢复后强制维护模式、任务暂停、外部服务关闭并撤销会话；重新启用前核对恢复时间点之后在商户侧实际发生的交易。

0.18生成的归档要求使用匹配的0.18代码读取。未来升级请同时保存原版本完整源码与归档，不假定所有归档格式跨版本自动兼容。

## 回滚

高版本数据库拒绝由旧程序启动。回滚必须停写并恢复同一时点的旧代码、数据库、配置密钥和文件，不可只覆盖回0.17的PHP。数据库回滚无法撤销真实付款、退款、代付、邮件或已经寄出的包裹。

更早版本保留顺序迁移路径，但本轮直接发布比对基线为0.17，不是所有历史版本生产数据库都经过升级演练。
